postgres {
  driver = "org.postgresql.Driver"
  host = "localhost"
  port = 5432
  user = "postgres"
  schema = "public"
  password = "postgres"
  jdbc-url = "jdbc:postgresql://"${postgres.host}":"${postgres.port}"/test_db"
  connections {
    pool-size = 10
  }
}
